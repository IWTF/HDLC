#include <stdio.h>
#include <math.h>
#include "MathFunctions.h"
#include "./math/tools.h"

int main () {
	int a = power1(2, 3);
	printf("++num is: %d\n", a);
	say_hello();
	puts("");
	printf("%lf\n", pow(4, 2));
	return 0;
}